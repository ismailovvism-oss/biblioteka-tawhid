# Changelog — Параллельная читалка

Хронология для самопогружения в контекст. Детали формата — `SPEC.md`, рабочий
контекст — `CLAUDE.md`. Новое сверху.

## 2026-07-16 — деплой уехал на GitHub Pages; валидатор чинён

### Репозиторий переименован: `chitalka-parallel` → `biblioteka-tawhid`
- Проект перерос «читалку» и стал библиотекой — имя приведено к бренду. GitHub редиректит
  старые URL (в т.ч. git), но **на всех клонах нужно `git remote set-url origin
  git@github.com:ismailovvism-oss/biblioteka-tawhid.git`** — иначе работа идёт через
  редирект, который однажды отвалится. Имя кеша SW (`chitalka-vNN`) намеренно не трогали:
  это внутренний ключ кеша, а не имя репо.

### Деплой: Netlify → GitHub Pages
- `.github/workflows/pages.yml` — **автодеплой на push в `master`**. На Netlify автодеплой
  был выключен (лимит кредитов) и выкладка шла вручную build-hook’ом; на Pages это
  бесплатно и автоматом. Сборки нет — репозиторий выкладывается как есть.
- В workflow перед выкладкой — `node --check` + `tools/validate.js` по всем книгам:
  **сломанный Контракт не доедет до прода**.
- **`.nojekyll`** в корне — обязателен: Jekyll по умолчанию игнорирует пути с `_`,
  то есть выбросил бы `books/_sample/` и `books/_sample-en/`.
- Адрес: `https://ismailovvism-oss.github.io/biblioteka-tawhid/` (свой домен не заводился).

### Валидатор: гибридные книги больше не роняют его
- `tools/validate.js` читал `<lang>/<file>` без проверки и падал с ENOENT на первой главе,
  которой нет на одном языке, — `hukm_majlis` (13 глав ru / 8 ar) вообще не проверялась.
  Теперь как в читалке (`loadChapterData`): нет файла → язык пустой, глава помечается
  «только ru». Настоящие ошибки Контракта ловятся по-прежнему (exit 1). Записи-`header`
  (глава-группа без `file`) пропускаются — на них был бы тот же ENOENT.

## 2026-07-16 — бренд: «Библиотека Алькасави» → «Библиотека Таухид» (sw `v32`)

Уход от персонального названия к нейтральному. Заменено везде, где имя выступало
**брендом**: `<title>` и заголовок панели «О программе» (`index.html`), PWA-манифест
(`name`/`short_name`), `title` реестра `books/index.json`, заголовок полки и подпись
внизу (`app.js`), доки (`CLAUDE.md`, `ARCHITECTURE.md`).

Следом (sw `v33`): **почта убрана с главной** — подпись внизу полки теперь просто
«Библиотека Таухид» (`textContent` вместо `innerHTML` — HTML там больше не нужен).
Адрес остался в «О приложении» и в `feedbackEmail` книг (кнопка «✉ Ошибка?» — это
не показ почты, а механика отправки; уберёшь — сломается отправка).

**Не тронуто намеренно** — имя как *атрибуция авторства переводов*, а не бренд:
строка «Автор и издатель переводов: Алькасави (qaadiy)» в «О программе», `authors`
в `index.json` и `author` в `books/hukm_majlis/book.json` (это же значение фасета
«Автор» в каталоге — правка порвала бы фильтр). Почта `qaadiy@gmail.com` тоже на месте.

## 2026-06-20 — переработка каталога + иерархия глав (sw `v27`→`v31`)

Большой блок. Каталог («полка») переписан, оглавление книги стало деревом.

### Таксономия каталога: `books/taxonomy.json` (sw `v27`)
- Дерево разделов вынесено из `index.json` в **`books/taxonomy.json`** — плоский
  список узлов `{ id, name, parent?, hue?, shade? }` (hue у L1 — цветовой род, shade
  у L2 — оттенок). Книга в `index.json` ссылается на раздел по **id листа**
  (`category: "aqida-iman-kufr"`), а не путём-массивом. `buildTaxonomy`/`catChain`/
  `catPathNames`/`bookCatPath`/`catColorOf` в `app.js`.

### Навигация: боковое дерево + хлебная крошка (sw `v28`)
- Категории-чипсы убраны; навигация = **боковое дерево** (`#cat-tree`,
  `renderTree`): десктоп ≥700px — постоянная колонка, телефон — выезжает по
  гамбургеру (`#btn-cattree`). Сворачиваемые узлы, счётчики. Крошка над списком.
- Карточка «Продолжить» → одна тонкая строка (только при сохранённой позиции).

### Фильтры в нижней панели (sw `v29`)
- Фасеты (Одобрение/Готовность/Язык/Автор/Мазхаб/Эпоха/Жанр/Тема) перенесены в
  выезжающий снизу bottom-sheet (`#filter-sheet`, кнопка-воронка `#btn-filter` с
  бейджем числа активных). `renderFilterSheet`/`updateFilterBadge`.

### Три вида списка книг (sw `v30`)
- Переключатель видов (`settings.shelfView`, дефолт `covers`): **Обложки**
  (auto-fit, ~3 кол. на телефоне), **Корешки** (цвет по категории `catColorOf` —
  hue рода + светлота от оттенка; ширина по объёму/хэшу 34–48px; высота по хэшу id
  88–100%; верхняя полоса = `review`; вертикальное рус. название; тап → карточка),
  **Список** (горизонтальные строки). `hashId`/`spineColor`/`spineDims`/`renderBookList`.

### Иерархия глав в книге: дерево оглавления (sw `v31`)
- `book.json.chapters` получил поля **`level` (0–3)** и записи-**`header`** (без
  `file` — группа-заголовок, напр. `كتاب`, двуязычная `title{ar,ru}`). `buildToc`→
  `renderTocList`: дерево с отступами, сворачиваемые группы, header-записи
  некликабельны; линейная навигация (`nextReadable`, btn-prev/next/клавиши/свайп)
  их пропускает. Обратная совместимость: нет `level` → 0 (плоско).

### Контент
- Книги размечены по дереву разделов; опубликована/обновлена `hukm_majlis`
  (деление глав → 13 глав). Лишние теги (`апологетика`/`опровержение клеветы`) убраны.

## 2026-06-19 — стрелки разделов: чётче + прячутся на краях (sw `v26`)
- `#btn-prev`/`#btn-next` крупнее и контрастнее (1.6rem, жирные, цвет акцента).
- На первой главе ‹ скрыта, на последней › скрыта (`updateChapterNav`, класс
  `.nav-hidden` через `visibility` — индикатор страницы не съезжает).

## 2026-06-19 — валидатор не мешает читателю (sw `v25`)
- Убрана проверка «глава целиком только в один язык» — для гибрида ru-only глава
  (вступление/проза) это норма, не предупреждение (`parser.js`).
- Значок ⚠ и панель валидатора — **только в режиме debug** (инструмент автора, не
  для читателя): `btn.hidden = !debug || !warnings`. Обычный читатель ⚠ не видит.

## 2026-06-19 — фиксы UX: главы без пары, страница, панель валидатора (sw `v24`)

### `app.js`
- **Глава без файла на одном языке больше не падает.** `loadChapterData`/`searchChapter`
  ловят 404 на язык-файл (гибрид: глава только на одном языке) → пустая строка, глава
  рендерится односторонней. Раньше — «Не удалось загрузить главу … HTTP 404».
- **Индикатор страницы:** нет маркера `<!-- pN -->` (статьи/главы без страниц) → не
  показываем «стр. —», только процент.
- **Панель валидатора** не рисует зелёную «ошибок не найдено» (зря занимала место):
  показывается только в debug И только когда есть реальные предупреждения.

## 2026-06-19 — дерево разделов (пустые видны) + валидатор hybrid-aware (sw `v23`)

### `app.js` / `style.css` — каталог: все объявленные разделы, даже пустые
- `index.json` → `categories: [[путь]…]` (объявленное дерево); каталог показывает
  все узлы, даже без книг (`catalogTree`), пустые — приглушённо пунктиром
  (`.cat-empty`, без счётчика). Книга появится — раздел оживёт.

### `parser.js` — непарные секторы больше НЕ ошибка контракта
- Убраны пер-секторные предупреждения «есть в ru/ar, нет пары» (строки 234–235).
  В гибридной модели непарность намеренна: ru-only = проза во всю ширину, ar-only =
  оригинал с меткой «идёт перевод». Реальные проверки (дубли якорей, сноски,
  «глава целиком на одном языке») остались. Значок ⚠ больше не пугает читателя
  ложными 18 на русско-первичных статьях.

### Контент
- Опубликована книга `hukm_majlis` (Хукм пребывания в маджлисе); образцы и тауфик
  размечены по дереву разделов + бейджи статуса. Поле `progress` (черновик/wip) у книг.

## 2026-06-19 — статус доверия книги: бейдж + фасет (sw `v20`)

### `app.js` / `style.css` — поле `review`
- `review` (одно значение): `approved` (✓ Одобрено), `caution` (⚠ Требует проверки),
  `draft` (🚧 В работе). `reviewBadge()` рисует **цветной бейдж** в углу обложки —
  на полке (иконка) и на карточке книги (иконка + подпись). `REVIEW`-словарь.
- Фасет **«Статус»** в `FACETS` (первым) — фильтр по тому же полю. Книга без `review`
  — без бейджа. Уровень книги (доверие для читателя), не путать со статусом раздела.

## 2026-06-19 — фасеты «Мазхаб» и «Жанр» (sw `v19`)

### `app.js` — два новых фасета каталога
- `FACETS` += `{madhhab:'Мазхаб'}` и `{genre:'Жанр'}` (поля `madhhab[]`/`genre[]`
  в записи `index.json`). Логика прежняя: показываются при ≥2 значениях, внутри
  группы ИЛИ, между группами И. Мазхаб (ханафи/малики/шафии/ханбали/захири/
  сравнительный) и жанр (рудуд/шарх/матн/фатавы) — ортогональны дереву разделов.
- Проставляются из формы публикации вычитки; дерево разделов (`category`) —
  иерархия любой глубины, см. `CLAUDE.md`.

## 2026-06-18 — сворачиваемые callout’ы (sw `v18`)

### `parser.js` / `style.css` — фолд callout’ов как в Obsidian
- `renderBlocks`: `> [!тип]+` → раскрытый сворачиваемый callout, `> [!тип]-` →
  свёрнутый; без знака — статичный `<div>` как раньше. Регэксп `[!тип]([+-]?)`.
  Сворачиваемые рендерятся как `<details class="callout-foldable">` + `<summary>`.
- CSS: маркер-стрелка у `<summary>`, тело свёрнутого скрыто явным правилом
  `details.callout-foldable:not([open]) > .callout-body{display:none}` (не полагаемся
  на UA-стиль `<details>`). Тот же словарь и рендер — в вычитке (редактор).

## 2026-06-16 — общий словарь с вычиткой, заметки, недопереведённые секторы

Вычитка (`translator-panel`) стала редактором этого формата «на месте»; читалка
доработана под общий контракт.

### `parser.js` — рендер форматирования (sw `v17`)
- `inlineMd`: подчёркивание `<u>…</u>`.
- `renderGroup`→`renderBlocks`: цитата `> ` → `<blockquote>`; списки `- `/`1. ` →
  `<ul>/<ol>`; callout `> [!тип] заголовок` → `.callout.callout-<тип>` (тип
  произвольный, дефолтная подпись из `CALLOUT_LABELS`, неизвестный тип — базовый стиль).
- `parseFile`: регионы `<!-- note --> … <!-- /note -->` пропускаются (личные
  редакторские заметки автора не публикуются). Парсер остаётся DOM-free.

### `app.js` / `style.css` — недопереведённые секторы
- `renderChapter`: текст-пара с одним показанным языком → `.solo` (во всю ширину);
  оригинал без перевода (в двуязычной книге) → `.awaiting` (пунктирная акцентная
  грань + значок «⟳», слово — в `title`, видно только при параллельном чтении).
- CSS форматирования (blockquote/списки/callout/`<u>`) — тема-зависимый.

### Зачем
Гибридные книги (моноязычный «хребет» + вставные параллельные цитаты) и публикация
недопереведённых книг: сектор только в оригинале = «перевод готовится», сектор только
в переводе = основной текст во всю ширину, сектор в обоих с одним id = параллель.
