# Непубличные книги. Лежат в папке проекта (видны локально, работают офлайн, читаются
# приложением как обычные), но в репозиторий не уходят: попав в коммит хотя бы раз,
# файл остаётся в публичной истории git навсегда — вычистить его оттуда уже нельзя.
# Вторая копия этих книг — в приватном бакете Supabase (BACKEND.md), она и даёт доступ
# с других устройств. Заливка — tools/upload-private.js.
#
# ⚠️ Не добавлять сюда книги через `git add -f`. Страховка от случайного коммита стоит
# в .github/workflows/pages.yml — деплой падает, если books-private/ попал под git.
books-private/

# служебное
.DS_Store
node_modules/
